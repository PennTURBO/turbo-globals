library(solrium)
library(readxl)
library(SPARQL)

# try to limit these global setup files
#   to package imports (using library())
#   and to function definitions

# the functions shouldn't really use any global variables,
#   but I have done that in some cases

print("from local file system")